# Frontend environment variables for SOW Generator (React)
# Copy to .env and set appropriate values.

# Supabase (already used elsewhere)
REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_KEY=
REACT_APP_SITE_URL=http://localhost:3000

# AI Integration
# Option A: Backend route preferred (no key here needed):
#   Implement an API at /api/ai/sow that accepts POST {prompt} and returns {sow: "..."}
#
# Option B: OpenAI fallback (client-side, for development only):
#   Set the following keys to enable direct OpenAI calls from the browser.
REACT_APP_OPENAI_API_KEY=
# Optional: choose a model, default is gpt-4o-mini
REACT_APP_OPENAI_MODEL=gpt-4o-mini

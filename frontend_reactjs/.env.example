# Optional: point frontend directly to backend if not using dev proxy
REACT_APP_BACKEND_URL=http://localhost:8080

# Supabase (optional for this project step)
REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_KEY=

# Optional OpenAI fallback (not needed; avoid to ensure no paid API usage)
# REACT_APP_OPENAI_API_KEY=
# REACT_APP_OPENAI_MODEL=gpt-4o-mini
